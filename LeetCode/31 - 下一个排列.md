> **比赛场次**：
> **比赛题目**： [31. 下一个排列](https://leetcode.cn/problems/next-permutation/) 

## 题意：

给你一整型数组，找出下一个排列。（按字典序升序）> **比赛场次**：
> **比赛题目**： 

<!--more-->
	2 seconds / 256 megabytes
	standard input / standard output

## 题意：

## 题解：

#标签

### 代码实现

> **时间复杂度：**$O()$
> **空间复杂度：**$O()$ 
## 其他


## 题解：

#思维 #middle 

类比：找出4399的下一个数
1.  从后向前找第一个不为9的数——3
2. 将3+1=4
3. 将其后99变为00
我们找下一个排列，类似这样的步骤。
例如：\[ 1,3,5,4,2 \] 
若 1,3,5 保持不变，只能重排 4,2 ，4 > 2 ，排列变小了，不行
若 1,3 保持不变, 5 > 4 > 2，不行
若 1 保持不变，3 < 4，故而可以让 3 这个位上的数字变大，由于下一个，故而是交换 4，剩下3个数一定递减，需要反转。

### 代码实现
```java
class Solution {
    public void nextPermutation(int[] nums) {
        int n = nums.length;

        // 第一步：从右向左找到第一个小于右侧相邻数字的数 nums[i]
        int i = n - 2;
        while (i >= 0 && nums[i] >= nums[i + 1]) {
            i--;
        }

        // 如果找到了，进入第二步；否则跳过第二步，反转整个数组
        if (i >= 0) {
            // 第二步：从右向左找到 nums[i] 右边最小的大于 nums[i] 的数 nums[j]
            int j = n - 1;
            while (nums[j] <= nums[i]) {
                j--;
            }
            // 交换 nums[i] 和 nums[j]
            swap(nums, i, j);
        }

        // 第三步：反转 [i+1, n-1]（如果上面跳过第二步，此时 i = -1）
        reverse(nums, i + 1, n - 1);
    }

    private void swap(int[] nums, int i, int j) {
        int tmp = nums[i];
        nums[i] = nums[j];
        nums[j] = tmp;
    }
    
    private void reverse(int[] nums, int left, int right) {
        while (left < right) {
            swap(nums, left++, right--);
        }
    }
}
```

> **时间复杂度：**$O(N)$
> **空间复杂度：**$O(1)$ 
## 其他
