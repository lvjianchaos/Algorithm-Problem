
给两个数比大小

```c++
#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main()
{
    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    double a,b;cin >> a >> b;
    cout << (a < b ? "YES\n" : "NO\n");
    return 0;
}
```